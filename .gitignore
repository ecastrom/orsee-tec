# Local environment / secrets — never commit.
.env
*.env.local

# Composer install output (Heroku runs its own install; we only track the lock).
/vendor/

# OS / editor cruft.
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# LaTeX build artifacts (docs/)
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
